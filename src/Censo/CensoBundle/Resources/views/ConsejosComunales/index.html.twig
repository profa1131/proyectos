{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ConsejosComunales list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Direccion</th>
                <th>Codigo</th>
                <th>Rif</th>
                <th>Numerocuenta</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('consejoscomunales_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.direccion }}</td>
                <td>{{ entity.codigo }}</td>
                <td>{{ entity.rif }}</td>
                <td>{{ entity.numeroCuenta }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('consejoscomunales_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('consejoscomunales_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('consejoscomunales_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
