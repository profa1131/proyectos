{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Parroquias creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('parroquias') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
